
<!DOCTYPE html>

<html>
  <head>
    <title>Is the building on fire?</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <% if @is_the_building_on_fire %>
        <marquee direction="right" behaviour="scroll" class="banner warning">
            The building <b>might be</b> currently on fire.
        </marquee>
    <% else %>
        <marquee direction="right" behaviour="scroll" class="banner allclear">
            The building <b>is not</b> currently on fire.
        </marquee>
    <% end %>
    
  

    <div class="flexbox">
        <div class="newsfeed">

            <div class="disclaimer block">
                This is a satirical website. If the fire alarms go off at 2am, please exit the building <b>before</b> checking this page</p>
            </div>
            <div class="flexbox">
                <% if @is_the_building_on_fire %>
                    <div class="status block warning">
                        The building <b>might be </b> on fire.
                    </div>
                <% else %>
                    <div class="status block allclear">
                        The building is <b>not</b> currently on fire.
                    </div>
                <% end %>
               

                <div class="posts">
                    Don't trust this website? Check out what our residents have to say!
                    <% @last_ten_statuses.each do |status| %>
                        <div class="post">
                            <%= status.message%>
                            <% if status.media.present? %>
                                <br>
                                <img src="<%= status.media %>"/>
                            <% end %>
                            <div class="author">
                                <%= status.author.present? ? status.author : "Anonymous" %> 
                                -
                                <%= status.nice_timestamp %>
                            </div>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
    

    <% if @is_the_building_on_fire %>
        <marquee direction="right" behaviour="scroll" class="banner warning">
            The building <b>might be</b> currently on fire.
        </marquee>
    <% else %>
        <marquee direction="right" behaviour="scroll" class="banner allclear">
            The building <b>is not</b> currently on fire.
        </marquee>
    <% end %>

  </body>
</html>
